import { History, RotateCcw, X } from 'lucide-react';
export interface Revision { id:string; createdAt:string; snapshot:{ name?:string; status?:string } }
export function RevisionPanel({ revisions,onRestore,onClose }:{ revisions:Revision[];onRestore:(id:string)=>void;onClose:()=>void }){return <div className="editor-backdrop"><section className="editor-panel revision-panel"><header className="editor-header"><div><p className="eyebrow">Recovery</p><h2>Course history</h2></div><button className="icon-button" aria-label="Close" onClick={onClose}><X/></button></header><div className="editor-body">{revisions.length===0?<div className="empty"><History/><h2>No revisions yet</h2><p>A revision is saved before every update or archive.</p></div>:<div className="revision-list">{revisions.map(r=><article className="nested-card revision" key={r.id}><div><strong dir="auto">{r.snapshot.name||'Untitled course'}</strong><span>{new Date(r.createdAt).toLocaleString()} · {r.snapshot.status||'Unknown status'}</span></div><button className="secondary" onClick={()=>onRestore(r.id)}><RotateCcw size={15}/> Restore</button></article>)}</div>}</div></section></div>}
