import { useState } from 'react';
import { api } from '../api.js';
export function Login({ onSuccess }: { onSuccess: () => void }) { const [password,setPassword]=useState(''); const [error,setError]=useState(''); const [busy,setBusy]=useState(false); async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');try{await api('/auth/login',{method:'POST',body:JSON.stringify({password})});onSuccess();}catch(err){setError(err instanceof Error?err.message:'Login failed');}finally{setBusy(false)}} return <main className="login-shell"><section className="login-card"><div className="brand-mark"><span>M</span></div><p className="eyebrow">Mustaner internal systems</p><h1>Course intelligence, kept precise.</h1><p className="muted">Manage the source of truth used by your sales agent.</p><form onSubmit={submit}><label className="field"><span>App password</span><input autoFocus type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password"/></label>{error&&<p className="error" role="alert">{error}</p>}<button className="primary wide" disabled={busy}>{busy?'Checking…':'Enter catalog'}</button></form></section></main> }
